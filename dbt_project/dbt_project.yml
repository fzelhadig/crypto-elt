name: 'dbt_project'
version: '1.0.0'
profile: 'dbt_project'

model-paths: ["models"]
target-path: "target"
clean-targets: ["target", "dbt_packages"]

models:
  dbt_project:
    staging:
      +materialized: view
    marts:
      +materialized: table